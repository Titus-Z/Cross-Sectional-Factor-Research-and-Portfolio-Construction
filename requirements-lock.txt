# Candidate direct-dependency lock for the public US300 release.
#
# Use together with requirements.txt:
#   python -m pip install -r requirements.txt -c requirements-lock.txt
#
# These versions match the local Python 3.12 research environment where
# metadata was available. Optional tree-model and mining pins remain candidates
# until their separate environments pass clean installation and smoke checks.
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
lightgbm==4.6.0
xgboost==3.0.0
matplotlib==3.9.2
seaborn==0.13.2
joblib==1.4.2
yfinance==0.2.66
tqdm==4.66.5
requests==2.33.0
pyarrow==23.0.1
tabulate==0.9.0
torch==2.13.0
