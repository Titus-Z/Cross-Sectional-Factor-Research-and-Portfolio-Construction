# US300 Public Evidence Package

> **Status: pending clean rerun.** This directory intentionally contains no
> performance number yet.

The canonical US300 training, final OOS audit, and long-short portfolio grid must
be rerun from one clean Git commit. `scripts/export_public_evidence.py` will replace
this placeholder only after validating the data, configuration, model, portfolio,
artifact hashes, and source provenance.

Until that export succeeds, the repository makes no current IC, return, Sharpe,
or drawdown claim.
